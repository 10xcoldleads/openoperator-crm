# Feature truth matrix

This matrix is the release contract for the open-source HighLevel-style workspace. A navigation item is visible only when its primary journey has persistence, authorization, failure handling, and automated acceptance evidence.

| Product surface | Current evidence | UI decision | Next complete vertical slice |
|---|---|---|---|
| Dashboard | Live metrics, briefing, scoring, pipeline snapshot, record drill-down | Visible | Add configurable dashboard widgets only with persisted layouts |
| Contacts | Create, search, filter, save views, bulk edit, import/rollback, companies, custom fields, timelines, manual E.164 phone maintenance, and owner-governed member phone editing | Visible | Continue browser acceptance across import and duplicate-resolution failures |
| Opportunities | Multiple pipelines, stage rules, drag/keyboard movement, editing, tasks, agent proposals, concurrency guards | Visible | Add forecast views only after API and acceptance tests exist |
| Calendar & tasks | Task CRUD/lifecycle plus bounded calendar range API and linked records | Visible | This module remains task-centric; Booking is a separate lifecycle |
| Booking | Draft/publish/revoke calendars, timezone-aware availability, buffers/notices, public booking, replay/conflict protection, private reschedule/cancel, appointment ledger, and hash-only management tokens | Visible | External calendar sync is explicitly local-only/false until a real provider adapter exists |
| Agent work | Scoped credentials, work queue, proposals, approvals, execution results, audit trail | Visible | Voice and unrestricted chat agents remain omitted |
| Automations | Versioned deterministic workflow builder, validation, activation, run history, repair paths, typed record fields, governed workspace custom values, and fail-closed merge-variable drift checks | Visible | SMS campaign/bulk actions remain absent; provider communication stays a separately governed boundary |
| App connections | Governed sources, signed inbound/outbound webhooks, mailboxes, scoped MCP credentials, encrypted Resend and Twilio credentials, provider verification, health checks, and revoke/rotate paths | Visible | Provider-specific setup wizards are added one tested adapter at a time; Twilio Console configuration is never claimed as API-controlled |
| Settings | Workspace access, roles, fields, objects, layouts, readiness, operations health, recovery | Visible | Domain and billing controls remain omitted |
| Conversations | Unified workspace-scoped email/SMS threads; Gmail/Outlook metadata sync; replay-safe Resend delivery; encrypted verified Twilio Messaging Service connection; E.164 validation; separate SMS consent evidence; express-consent gate for marketing; templating from standard fields, typed custom fields, and workspace custom values; signed inbound/status callbacks; STOP/START/HELP handling; monotonic delivery states; quarantined unknown senders; and lifecycle controls | Visible | MMS, voice, WhatsApp, bulk SMS, schedules, number purchasing, A2P registration, and Twilio Advanced Opt-Out configuration remain provider-console responsibilities |
| Forms | Versioned form definitions, immutable published snapshots, public responsive intake, required privacy acknowledgement, optional express-email consent, rate/replay/honeypot controls, immediate revocation, contact/activity association, and submission ledger | Visible | Add richer field/layout types only with equivalent validation, recovery, and browser evidence |
| Surveys | Versioned drafts and immutable published snapshots, stable IDs and six bounded question types, multi-step public response flow, required privacy acknowledgement without marketing opt-in, replay/rate/honeypot controls, admin-only mutation lifecycle, version-faithful aggregates, immutable response ledger, recovery coverage, and responsive browser proof | Visible | Richer question/branching types require equivalent validation and historical-version evidence |
| Sites | Workspace-scoped drafts; 1–10 ordered pages; bounded hero/text/features/CTA components; safe links and themes; editable SEO metadata; admin-only optimistic/audited lifecycle; immutable publication versions; responsive hosted-path rendering and navigation; immediate revoke; recovery coverage; custom domains and arbitrary code explicitly inactive | Visible | Custom domains remain absent until ownership, collision, TLS, routing, rollback, and removal are independently implemented and proven |
| Estimates | Admin-only Contact-linked drafts; deterministic integer minor-unit totals; validated expiry; optimistic editing; one immutable frozen version; one-time hash-only fragment access; non-signature acknowledgement/decline with privacy evidence; stable retry key; rate/honeypot controls; immediate revoke; audit/recovery coverage; real Edge desktop and 390px mobile proof | Visible | E-signature, contracts, invoices, tax, checkout, payment providers, PDF generation, and automatic delivery remain explicitly absent |
| Payments | Admin-only append-only manual ledger; contact/opportunity links; idempotent payment ingestion; immutable payment/refund/dispute/reversal events; bounded adjustments; currency-separated balances; audit/recovery coverage; two-step confirmations; responsive browser proof | Visible | Stripe, banking, invoices, payouts, settlements, FX conversion, and provider reconciliation remain explicitly absent until each adapter has equivalent credential, webhook, replay, failure, and browser evidence |
| Marketing email | Admin-only bounded plain-text campaigns through an active verified Resend connection; real `con_…` Contact selection; current express-consent filtering; immutable recipient snapshots; per-recipient provider evidence; pre-send consent recheck; retry/cancel controls; hash-only fragment-delivered one-click unsubscribe; atomic suppression/audit; recovery coverage; responsive browser proof | Visible | Provider webhooks, delivery synchronization, HTML, tracking, schedules, social publishing, and high-volume queues remain absent until independently implemented and proven |
| Social marketing | No provider authorization, publication lifecycle, scheduler, or post evidence | Omitted | Research and implement as a separate provider-bounded module |
| Review requests | Admin-only credential-free HTTPS destination configuration; current express-consent Contact enforcement; active verified Resend requirement; immutable one-request snapshots; provider failure/retry evidence; separate hash-only fragment feedback/opt-out credentials; one immutable private 1–5 response; privacy acknowledgement; replay/rate/honeypot controls; rating-independent public-review link; atomic opt-out/audit; recovery validation; responsive browser proof | Visible | Third-party provider ownership, ingestion, synchronization, public replies, incentives, bulk sends, and schedules remain absent until independently implemented and proven |
| Reputation | No review-provider adapter or reply governance | Omitted | Add only with provider evidence and human approval for replies |
| Reporting | Bounded 7/30/90-day first-party cohorts, daily arrivals, contact lifecycle distribution, directional first-touch grouping, permission-aware pipeline snapshots, currency-separated values, responsive browser proof, and explicit methodology limits | Visible | Historical conversion requires a dedicated immutable stage-transition event model; ads/calls remain omitted until authoritative sources exist |

## Proof rules

- A card, route, button, or placeholder is not a feature.
- “Visible” requires a working primary browser journey and negative authorization tests.
- External integrations require per-workspace credentials, revocation, redacted logs, idempotency, and timeout/retry behavior.
- Open-source dependencies require license compatibility, maintainer/release history review, dependency scanning, and a written reason to adopt instead of build.
- Missing modules stay absent from navigation. Documentation may name them only as planned work.
